import client
    class Order (client.Client):
        def_init_(self, name, status, total_price , is_active)
            self.name = name
            self.status = status
            self.total_price = total_price
            self.is_active = is_active